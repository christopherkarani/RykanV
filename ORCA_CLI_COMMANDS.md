# Orca CLI Command Reference (compat stub)

**Primary brand is now [ryk](./RYK_CLI_COMMANDS.md).**

The product CLI binary is **`ryk`**. The **`orca`** name remains a PATH alias of the same binary for at least one major version.

- Full command reference: [`RYK_CLI_COMMANDS.md`](./RYK_CLI_COMMANDS.md)
- Workspace discovery paths still use `.orca/` until Phase 5b path migration.
- Env: prefer `RYK_*`, fall back `ORCA_*` where dual-read is implemented.

This file is kept so old links and bookmarks continue to resolve during the dual-name window.
