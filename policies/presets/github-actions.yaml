# ryk preset: github-actions
# CI-safe preset. CI mode never prompts; ask-class decisions are denied unless explicit allow rules are added.
# Do not put workflow tokens or repository secrets in this policy.
version: 1
mode: ci

workspace:
  root: "."
  write_mode: staged

env:
  inherit: false
  allow:
    - PATH
    - HOME
    - LANG
    - TERM
    - CI
    - GITHUB_ACTIONS
    - GITHUB_WORKSPACE
    - GITHUB_REPOSITORY
    - GITHUB_RUN_ID
    - GITHUB_REF
    - GITHUB_SHA
  deny_patterns:
    - "*TOKEN*"
    - "*SECRET*"
    - "*PASSWORD*"
    - "*PASSWD*"
    - "*PRIVATE*"
    - "*KEY*"
    - "AWS_*"
    - "AZURE_*"
    - "GITHUB_TOKEN"
    - "GH_TOKEN"
    - "OPENAI_API_KEY"
    - "ANTHROPIC_API_KEY"
    - "NPM_TOKEN"
    - "PYPI_TOKEN"
    - "SSH_AUTH_SOCK"

files:
  read:
    allow:
      - "./**"
    deny:
      - "./.env"
      - "./.env.*"
      - "~/.ssh/**"
      - "~/.aws/**"
      - "~/.config/gh/**"
      - "**/*credentials*"
      - "**/*secret*"
      - "**/*token*"
  write:
    allow:
      - "./**"
    deny:
      - "./.git/**"
      - "./.ryk/**"
    mode: staged

commands:
  default: deny
  allow:
    - "git status"
    - "git diff *"
    - "ls *"
    - "pwd"
    - "zig version"
    - "zig build"
    - "zig build test"
    - "./scripts/agent-task.sh"
  deny:
    - "rm -rf *"
    - "curl * | sh"
    - "wget * | bash"
    - "sudo *"
    - "cat .env"
    - "cat ~/.ssh/*"

network:
  mode: allowlist
  default: deny
  allow:
    - "api.github.com"
    - "*.github.com"
  deny:
    - "pastebin.com"
    - "*.ngrok.io"
    - "*.requestbin.net"
  detect_exfiltration:
    dns: true
    long_query_strings: true
    secret_patterns: true

mcp:
  default: deny
  allow:
    - "*.list_*"
    - "*.get_*"
    - "*.search_*"
  deny:
    - "*.delete_*"
    - "*.shell"
    - "*.run_command"

audit:
  level: full
  redact_secrets: true
  tamper_evident: true
