# Shortcuts

The release Shortcut is built by hand in **Shortcuts.app** (not checked in as a binary).

Follow: [`docs/dev/cut-release-shortcut.md`](../../docs/dev/cut-release-shortcut.md).

The Shortcut only provides confirm UX; all logic lives in `scripts/cut-release.sh`.
