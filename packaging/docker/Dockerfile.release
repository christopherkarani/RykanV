# syntax=docker/dockerfile:1
# CLI-only release image (Zig shell_engine in-process; no ryk-daemon).
# Canonical binary: ryk.
FROM debian:bookworm-slim AS build

ARG TARGETARCH
ARG RYK_VERSION
ARG RYK_COMMIT
ARG RYK_BUILD_DATE

RUN apt-get update \
    && apt-get install -y --no-install-recommends ca-certificates curl file xz-utils \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /src
COPY . .

RUN version="${RYK_VERSION}" \
    && commit="${RYK_COMMIT}" \
    && build_date="${RYK_BUILD_DATE}" \
    && case "$TARGETARCH" in \
      amd64) zig_target=x86_64-linux ;; \
      arm64) zig_target=aarch64-linux ;; \
      *) echo "unsupported Docker target architecture: $TARGETARCH" >&2; exit 1 ;; \
    esac \
    && ./scripts/zig build install-ryk \
      -Dtarget="$zig_target" \
      -Doptimize=ReleaseSafe \
      -Dversion="$version" \
      -Dcommit="$commit" \
      -Dbuild-date="$build_date" \
      --prefix /tmp/ryk-cli \
    && mkdir -p "/out/linux-$TARGETARCH" \
    && test -x /tmp/ryk-cli/bin/ryk \
    && cp -p /tmp/ryk-cli/bin/ryk "/out/linux-$TARGETARCH/ryk"

FROM scratch
COPY --from=build /out/ /
