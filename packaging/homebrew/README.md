# Legacy Homebrew template

Homebrew is not an active Rykan V distribution channel. Releases publish the
GitHub assets consumed by the checksum-verified curl installer.

This directory remains only as a legacy packaging input. Do not use it for a
release or user installation.
