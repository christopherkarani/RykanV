# Legacy npm template

npm is not an active Rykan V distribution channel. Releases publish the GitHub
assets consumed by the checksum-verified curl installer.

This directory remains only as a legacy packaging input. Do not publish it or
use it as the user installation path.
