{
  "SessionStart": [
    {
      "matcher": ".*",
      "hooks": [
        {
          "type": "command",
          "command": "sh -c 'if command -v ryk >/dev/null 2>&1; then exec ryk hook claude SessionStart; elif command -v orca >/dev/null 2>&1; then exec orca hook claude SessionStart; else exec ryk hook claude SessionStart; fi'",
          "timeout": 10,
          "statusMessage": "Checking ryk plugin status"
        }
      ]
    }
  ],
  "UserPromptSubmit": [
    {
      "matcher": ".*",
      "hooks": [
        {
          "type": "command",
          "command": "sh -c 'if command -v ryk >/dev/null 2>&1; then exec ryk hook claude UserPromptSubmit; elif command -v orca >/dev/null 2>&1; then exec orca hook claude UserPromptSubmit; else exec ryk hook claude UserPromptSubmit; fi'",
          "timeout": 10,
          "statusMessage": "Checking prompt with ryk"
        }
      ]
    }
  ],
  "PreToolUse": [
    {
      "matcher": ".*",
      "hooks": [
        {
          "type": "command",
          "command": "sh -c 'if command -v ryk >/dev/null 2>&1; then exec ryk hook claude PreToolUse; elif command -v orca >/dev/null 2>&1; then exec orca hook claude PreToolUse; else exec ryk hook claude PreToolUse; fi'",
          "timeout": 15,
          "statusMessage": "Checking tool use with ryk"
        }
      ]
    }
  ],
  "PermissionRequest": [
    {
      "matcher": ".*",
      "hooks": [
        {
          "type": "command",
          "command": "sh -c 'if command -v ryk >/dev/null 2>&1; then exec ryk hook claude PermissionRequest; elif command -v orca >/dev/null 2>&1; then exec orca hook claude PermissionRequest; else exec ryk hook claude PermissionRequest; fi'",
          "timeout": 15,
          "statusMessage": "Checking permission request with ryk"
        }
      ]
    }
  ],
  "PostToolUse": [
    {
      "matcher": ".*",
      "hooks": [
        {
          "type": "command",
          "command": "sh -c 'if command -v ryk >/dev/null 2>&1; then exec ryk hook claude PostToolUse; elif command -v orca >/dev/null 2>&1; then exec orca hook claude PostToolUse; else exec ryk hook claude PostToolUse; fi'",
          "timeout": 10,
          "statusMessage": "Acknowledging tool use to ryk"
        }
      ]
    }
  ],
  "SessionEnd": [
    {
      "matcher": ".*",
      "hooks": [
        {
          "type": "command",
          "command": "sh -c 'if command -v ryk >/dev/null 2>&1; then exec ryk hook claude SessionEnd; elif command -v orca >/dev/null 2>&1; then exec orca hook claude SessionEnd; else exec ryk hook claude SessionEnd; fi'",
          "timeout": 10,
          "statusMessage": "Notifying ryk of session end"
        }
      ]
    }
  ]
}
