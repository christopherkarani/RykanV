{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ryk.local/schemas/host-capabilities-v1.json",
  "title": "ryk Host Capabilities v1",
  "description": "Capability report exposed to host plugins so they know what ryk can and cannot enforce.",
  "type": "object",
  "required": ["version", "ryk_version", "decisions", "limitations", "ci_mode"],
  "properties": {
    "version": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1
    },
    "ryk_version": {
      "type": "string"
    },
    "decisions": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["command", "file", "prompt", "tool", "network", "mcp"]
      },
      "description": "Decision kinds supported by this ryk build."
    },
    "limitations": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Honest capability limits."
    },
    "ci_mode": {
      "type": "boolean",
      "description": "Whether ryk is running in CI mode (never prompts)."
    },
    "sandbox_level": {
      "type": "string",
      "enum": ["none", "wrapper-only", "observe-only", "limited", "active"],
      "description": "Best available sandbox level on this platform."
    },
    "tool_ask_enforcement": {
      "type": "string",
      "enum": [
        "native_approve_and_resume",
        "hard_block_no_resume",
        "advisory_only",
        "none"
      ],
      "description": "How this host enforces ryk ask on tool events. native_approve_and_resume is the preferred long-term path."
    },
    "prompt_ask_enforcement": {
      "type": "string",
      "enum": [
        "native_approve_and_resume",
        "hard_block_no_resume",
        "advisory_context_only",
        "outer_wrapper_only",
        "none"
      ],
      "description": "How this host enforces ryk ask on prompt/pre-LLM events. advisory_context_only must not be advertised as approval."
    },
    "warn_collapses_to_block": {
      "type": "boolean",
      "description": "If true, warn is intentionally treated as hard block (must be documented and tested)."
    }
  },
  "additionalProperties": false
}
